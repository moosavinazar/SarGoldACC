<Window x:Class="SarGoldACC.WpfApp.Views.RcvMelted"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:SarGoldACC.WpfApp.Control"
        xmlns:views="clr-namespace:SarGoldACC.WpfApp.Views"
        xmlns:converters="clr-namespace:SarGoldACC.WpfApp.Converter"
        mc:Ignorable="d"
        Title="پرداخت آبشده" 
        Height="250" 
        Width="400"
        KeyDown="Window_KeyDown"
        Focusable="True"
        Loaded="Window_Loaded"
        Activated="Window_Activated"
        FlowDirection="RightToLeft"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen"
        Background="Aqua">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <controls:ComboBoxSelector 
                x:Name="LaboratoryComboBox" 
                Grid.Column="0"
                LostFocus="LaboratorySelectorControl_LostFocus"
                Label="دسته بندی"
                ItemsSource="{Binding Laboratories, Mode=TwoWay}"
                SelectedValue="{Binding LaboratoryId, Mode=TwoWay}"
                DisplayMemberPath="Name"
                SelectedValuePath="Id"
                CanAccessAddButton="{Binding CanAccessLaboratoryButton}"
                AddWindowType="{x:Type views:MadeSubCategory}"/>
            <controls:ComboBoxSelector 
                x:Name="BoxComboBox" 
                Grid.Column="1"
                LostFocus="BoxSelectorControl_LostFocus"
                Label="جعبه"
                ItemsSource="{Binding Boxes, Mode=TwoWay}"
                SelectedValue="{Binding BoxId, Mode=TwoWay}"
                DisplayMemberPath="Name"
                SelectedValuePath="Id"
                CanAccessAddButton="{Binding CanAccessBoxButton}"
                AddWindowType="{x:Type views:Box}"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <controls:TextBoxValidate 
                x:Name="Ang" 
                Grid.Column="0"
                Label="شماره انگ"
                ValidText="{Binding Ang, Mode=TwoWay}"
                ValidTextPattern="^.+$"
                ValidTextFinalPattern="^.+$"
                NotValidTextMessage="انگ الزامی است"
                AllowNullText="False"
                InputLanguage="English"/>
            <TextBlock Text="شرطی؟"
                       Grid.Column="1"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="10,5,5,0"/>
            <CheckBox x:Name="IsActiveCheckBox"
                      Grid.Column="2"
                      VerticalAlignment="Center"
                      Margin="0,5,5,0"
                      IsChecked="{Binding Certain}"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <controls:TextBoxValidate 
                x:Name="Ayar" 
                Grid.Column="0"
                Label="عیار"
                ValidText="{Binding Ayar, Mode=TwoWay}"
                ValidTextPattern="^[0-9]+$"
                ValidTextFinalPattern="^|(\d{3})$"
                TextLength="3"
                NotValidTextMessage="عیار الزامی است"
                AllowNullText="True"
                InputLanguage="English"/>
            <controls:TextBoxWeight
                x:Name="Weight" 
                Grid.Column="1"
                Ayar="{Binding Ayar, Mode=TwoWay}"
                Weight750="{Binding Weight750, Mode=TwoWay}"
                Label="وزن"
                ValidText="{Binding Weight, Mode=TwoWay}"/>
            <controls:TextBoxWeight
                IsEnabled="False"
                x:Name="Weight750" 
                Grid.Column="2"
                Label="750"
                ValidText="{Binding Weight750, Mode=TwoWay}"/>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <controls:TextBoxValidate 
                x:Name="Description" 
                Grid.Column="0"
                Label="توضیحات"
                ValidText="{Binding Description, Mode=TwoWay}"
                ValidTextPattern="^*$"
                ValidTextFinalPattern="^*$"
                NotValidTextMessage="توضیحات اختیاری است"
                AllowNullText="True"
                InputLanguage="Persian"/>
        </Grid>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button x:Name="SaveButton"
                    IsEnabled="{Binding CanSave}"
                    Content="ذخیره"
                    Style="{StaticResource SuccessBtn}"
                    HorizontalAlignment="Center"
                    Click="ClickSave"
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>
