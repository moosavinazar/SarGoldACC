<UserControl x:Class="SarGoldACC.WpfApp.Controls.CustomDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SarGoldACC.WpfApp.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DataGrid x:Name="MainDataGrid"
              ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
              AutoGenerateColumns="False"
              CanUserResizeColumns="True"
              CanUserSortColumns="True"
              IsReadOnly="True"
              HeadersVisibility="Column"
              CanUserAddRows="False"
              SelectionMode="Single"
              ContextMenuOpening="DataGrid_ContextMenuOpening">
        <DataGrid.ContextMenu>
            <ContextMenu x:Name="DataGridContextMenu"
                         DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                <MenuItem Header="ستون‌ها">
                    <ItemsControl ItemsSource="{Binding ColumnSettings}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding DisplayName}"
                                          IsChecked="{Binding IsVisible, UpdateSourceTrigger=PropertyChanged}"
                                          Margin="5,0"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </MenuItem>
            </ContextMenu>
        </DataGrid.ContextMenu>
    </DataGrid>
</UserControl>

